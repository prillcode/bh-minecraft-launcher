---
phase: 07-imported-instance-type
plan: 07-02
type: execute
---

<objective>
Wire the 'imported' instance type into the UI — create/edit modals, instance card,
and sidebar play area.

Purpose: Users can create an imported instance by picking an existing game directory,
see it clearly distinguished from server/singleplayer instances in the list, and
launch it from the sidebar.

Output: CreateInstanceModal with directory picker flow for imported type; EditInstanceModal
showing read-only path for imported; InstanceList card with folder icon and directory
path; Sidebar third-line label reading "Local Install" for imported instances.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/pcw-106-ux-improvements-v1/BRIEF.md
@src/renderer/components/instances/CreateInstanceModal.tsx
@src/renderer/components/instances/EditInstanceModal.tsx
@src/renderer/components/instances/InstanceList.tsx
@src/renderer/components/layout/Sidebar.tsx
@src/renderer/global.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CreateInstanceModal — imported type flow</name>
  <files>src/renderer/components/instances/CreateInstanceModal.tsx</files>
  <action>
    Add 'imported' to the instanceType state type and the type select dropdown:
      <option value="imported">Imported (existing install)</option>

    Add state for the picked directory path:
      const [importedDir, setImportedDir] = useState('');

    When instanceType === 'imported', replace the server fields section with:
    - A "Choose Folder" button that calls window.launcher.instances.pickDirectory()
      and sets both importedDir and (if name is empty) auto-populates the name field
      from path.basename(selectedPath)
    - A read-only display of the selected path below the button (small, muted text)
    - If no directory is selected yet, show hint text: "Select the game directory of
      your existing Minecraft install"

    In handleSubmit, when type === 'imported':
    - Validate that importedDir is set; show error "Please select a game directory" if not
    - Pass gameDirectory: importedDir in the config object alongside type: 'imported'
    - Do NOT pass serverAutoConnect

    Keep existing server/singleplayer branches unchanged.

    Note: window.launcher.instances.pickDirectory() returns Promise<string | null>.
    Handle the null case (user cancelled dialog) by doing nothing — don't clear the
    previously selected path.
  </action>
  <verify>pnpm tsc -p tsconfig.renderer.json --noEmit</verify>
  <done>No type errors; imported option in dropdown; directory picker wired; name auto-populates; validation present</done>
</task>

<task type="auto">
  <name>Task 2: EditInstanceModal — imported type display</name>
  <files>src/renderer/components/instances/EditInstanceModal.tsx</files>
  <action>
    Add 'imported' to the instanceType state initializer:
      instance.type === 'singleplayer' ? 'singleplayer'
      : instance.type === 'imported' ? 'imported'
      : 'server'

    Add 'imported' to the type select dropdown (same option label as CreateInstanceModal).

    When instanceType === 'imported', hide the server fields section (same condition
    as singleplayer). Additionally show a read-only directory path field:
      <div className="form-group">
        <label>Game Directory</label>
        <input type="text" value={instance.gameDirectory} readOnly
               style={{ opacity: 0.7, cursor: 'default' }} />
        <p className="form-group__hint">
          Directory cannot be changed after import. Delete and re-import to use a
          different folder.
        </p>
      </div>

    In handleSubmit, when instanceType === 'imported', ensure serverAutoConnect and
    serverMinecraftVersion are cleared (set to undefined), same as singleplayer.

    Do NOT allow changing the gameDirectory via this form — it's display-only.
  </action>
  <verify>pnpm tsc -p tsconfig.renderer.json --noEmit</verify>
  <done>No type errors; imported shown in type select; server fields hidden; game directory displayed read-only</done>
</task>

<task type="auto">
  <name>Task 3: InstanceList card — imported icon and directory display</name>
  <files>src/renderer/components/instances/InstanceList.tsx</files>
  <action>
    In the instance card icon block, add imported case:
      inst.type === 'singleplayer' ? '🌳'
      : inst.type === 'imported' ? '📁'
      : (ping favicon or '🌍')

    In the card info block, extend the type branch to handle imported alongside
    singleplayer (both show path in the motd row, no ping):
      {inst.type === 'singleplayer' || inst.type === 'imported' ? (
        <>
          <span className="instance-card__server">
            {inst.type === 'imported' ? 'Local Install' : 'Singleplayer'}
          </span>
          <span className="instance-card__motd">{inst.gameDirectory}</span>
        </>
      ) : (
        /* existing server branch */
      )}

    In the ping guard conditions (load, handleCreated, handleUpdated), the existing
    check `inst.type !== 'singleplayer'` must also exclude 'imported'. Change all
    three to:
      inst.type === 'server' && inst.serverAutoConnect
    This is more explicit and future-proof than negation.
  </action>
  <verify>pnpm tsc -p tsconfig.renderer.json --noEmit</verify>
  <done>No type errors; imported card shows 📁 icon, "Local Install" label, and gameDirectory path; imported instances not pinged</done>
</task>

<task type="auto">
  <name>Task 4: Sidebar — imported type label</name>
  <files>src/renderer/components/layout/Sidebar.tsx</files>
  <action>
    In the sidebar__play-type span that shows the connection type, add the imported case:
      selectedInstance.type === 'singleplayer' ? 'Singleplayer'
      : selectedInstance.type === 'imported' ? 'Local Install'
      : selectedInstance.serverAutoConnect
        ? `Multiplayer — ${selectedInstance.serverAutoConnect.host}`
        : 'Multiplayer'
  </action>
  <verify>pnpm tsc -p tsconfig.renderer.json --noEmit</verify>
  <done>No type errors; imported selected instance shows "Local Install" as third line above Play button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full imported instance UI — create flow with directory picker, edit modal with
    read-only path, instance card with 📁 icon and "Local Install" label, sidebar
    "Local Install" label. Singleplayer and server instances unchanged.
  </what-built>
  <how-to-verify>
    1. Run: pnpm dev:all
    2. Open Instances tab → click + New Instance
    3. Select "Imported (existing install)" from the Instance Type dropdown
    4. Confirm: server fields disappear; "Choose Folder" button appears
    5. Click "Choose Folder" — native directory picker opens
    6. Select a directory — path appears below button; name field auto-populates
    7. Create the instance — appears in list with 📁 icon, "Local Install" label, path shown
    8. Select it — sidebar shows instance name, version, "Local Install"
    9. Click the edit (⚙) button — type shows "Imported", game directory shown read-only,
       server fields absent
    10. Verify singleplayer and server instances still display correctly
  </how-to-verify>
  <resume-signal>Type "approved" to continue to commit, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit UI changes</name>
  <files>src/renderer/components/instances/CreateInstanceModal.tsx, src/renderer/components/instances/EditInstanceModal.tsx, src/renderer/components/instances/InstanceList.tsx, src/renderer/components/layout/Sidebar.tsx</files>
  <action>
    Stage and commit all four files with message:
    "feat(07): imported instance UI — create picker, edit read-only, card + sidebar labels"
  </action>
  <verify>git log --oneline -1</verify>
  <done>Commit exists with all four files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm tsc -p tsconfig.renderer.json --noEmit passes
- [ ] Imported instance can be created via directory picker
- [ ] Name auto-populates from folder basename
- [ ] Card shows 📁 icon, "Local Install", and gameDirectory path
- [ ] Edit modal shows read-only path, no server fields
- [ ] Sidebar shows "Local Install" for imported instances
- [ ] Server and singleplayer instances unaffected
- [ ] No server ping attempted for imported instances
</verification>

<success_criteria>
- All tasks completed and committed
- Human verify checkpoint passed
- Zero TypeScript errors
- All three instance types visually distinguishable
</success_criteria>

<output>
After completion, create `.planning/pcw-106-ux-improvements-v1/phases/07-imported-instance-type/07-02-SUMMARY.md`
using the summary template. Note any deviations from this plan.

Next: 07-03-PLAN.md (Help docs audit + update)
</output>
