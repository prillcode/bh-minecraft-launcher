# Plan: 04-01 — Quilt Mod Loader Support (verify + fill gaps)

**Phase:** 04 — Quilt Mod Loader Support
**Work:** pcw-106 ux-improvements-v1

## Context

Most of Quilt support was already wired in a prior session:
- `src/core/game/quilt-provisioner.ts` — exists
- `src/core/game/launch.ts` — already imports `QuiltProvisioner`, has `'quilt'` branch (lines 45-48)
- `src/core/game/types.ts` — `DownloadProgress.phase` already includes `'quilt'`
- `src/renderer/components/instances/EditInstanceModal.tsx` — already has Quilt dropdown option
- `src/renderer/global.d.ts` — `InstanceConfig.modLoader` union already includes `'quilt'`
- `src/renderer/components/shaders/ShadersTab.tsx` — already gates on `fabric || quilt`

This plan is a verification pass to confirm everything compiles and works end-to-end,
and to fill any remaining gaps (likely `CreateInstanceModal`).

## Steps

### Step 1 — Verify `quilt-provisioner.ts` is complete

**File:** `src/core/game/quilt-provisioner.ts`

Read the file and confirm it:
- Uses `https://meta.quiltmc.org/v3/versions/loader/{gameVersion}` to fetch loader list
- Uses `https://meta.quiltmc.org/v3/versions/loader/{gameVersion}/{loaderVersion}/profile/json` for profile
- Has the same `provision(gameVersion, onProgress)` → `{ mainClass, libraryPaths }` signature as FabricProvisioner
- Reports `phase: 'quilt'` in progress events
- Uses `mavenCoordToPath` for library paths

If any of these are missing, fix them by following FabricProvisioner exactly with Quilt URLs.

---

### Step 2 — Check `CreateInstanceModal` for Quilt option

**File:** `src/renderer/components/instances/CreateInstanceModal.tsx`

Read the file. Confirm the mod loader `<select>` includes:
```tsx
<option value="quilt">Quilt</option>
```

If missing, add it alongside the existing Fabric option.
Also confirm the `modLoader` state type includes `'quilt'`.

---

### Step 3 — Build and verify TypeScript

```bash
pnpm dev:all
```

Confirm zero TypeScript errors. If `quilt-provisioner.ts` is incomplete, the import in `launch.ts`
will produce a type error pointing to the missing export — fix accordingly.

---

### Step 4 — Smoke test

- Create an instance with Quilt mod loader
- Launch it: status bar should show `quilt` phase download progress
- Confirm game launches with Quilt main class (check logs)
- Confirm Mods tab is enabled (not blocked by isVanilla)
- Confirm Shaders tab is enabled (isSupported = true for quilt)

---

## Verification Checklist

- [ ] `quilt-provisioner.ts` exists and exports `QuiltProvisioner` with correct signature
- [ ] `CreateInstanceModal` has Quilt option in mod loader dropdown
- [ ] `pnpm dev:all` builds with zero TypeScript errors
- [ ] Quilt instance launches successfully; status bar shows quilt progress phase
- [ ] Mods tab works for Quilt instances (not blocked)
- [ ] Shaders tab works for Quilt instances (not blocked)
