---
phase: 02-settings-ui
plan: 02-01
type: execute
depends_on: 01-01
---

<objective>
Build the SettingsPage.tsx component with five sections (Account, Java & Performance, Game, About, BlockHaven), add settings-specific CSS, and wire the /settings route in App.tsx.
Purpose: Replaces the placeholder "Settings (coming soon)" div with a fully functional settings page.
</objective>

<context>
@.planning/pcw-105-build-out-settings-tab/BRIEF.md
@.planning/pcw-105-build-out-settings-tab/phases/01-settings-backend/01-01-SUMMARY.md
@src/renderer/global.d.ts
@src/renderer/stores/auth-store.ts
@src/renderer/styles/globals.css
@src/renderer/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settings-specific CSS to globals.css</name>
  <files>src/renderer/styles/globals.css</files>
  <action>
Append a new `/* ── Settings page ── */` section at the end of globals.css with these rules:

**Layout:**
```css
.settings { padding: 24px 32px; max-width: 680px; }

.settings__section {
  margin-bottom: 32px;
  padding: 20px 24px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
}

.settings__section-title {
  font-size: 13px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: 16px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--border-subtle);
}

.settings__row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 10px 0;
}

.settings__row + .settings__row {
  border-top: 1px solid var(--border-subtle);
}

.settings__label {
  font-size: 13px;
  color: var(--text-primary);
  font-weight: 500;
}

.settings__hint {
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 2px;
}
```

**Toggle switch:**
```css
.settings__toggle {
  position: relative;
  width: 40px;
  height: 22px;
  flex-shrink: 0;
}

.settings__toggle input {
  opacity: 0;
  width: 0;
  height: 0;
  position: absolute;
}

.settings__toggle-track {
  position: absolute;
  inset: 0;
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  border-radius: 11px;
  cursor: pointer;
  transition: background 0.2s, border-color 0.2s;
}

.settings__toggle input:checked + .settings__toggle-track {
  background: var(--accent);
  border-color: var(--accent);
}

.settings__toggle-track::after {
  content: '';
  position: absolute;
  top: 2px;
  left: 2px;
  width: 16px;
  height: 16px;
  background: var(--text-muted);
  border-radius: 50%;
  transition: transform 0.2s, background 0.2s;
}

.settings__toggle input:checked + .settings__toggle-track::after {
  transform: translateX(18px);
  background: var(--bg-primary);
}
```

**Memory sliders:**
```css
.settings__slider-row {
  padding: 10px 0;
}

.settings__slider-row + .settings__slider-row {
  border-top: 1px solid var(--border-subtle);
}

.settings__slider-header {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 8px;
}

.settings__slider-value {
  font-size: 12px;
  color: var(--accent);
  font-weight: 600;
  font-family: var(--font-mono);
}

.settings__slider {
  width: 100%;
  -webkit-appearance: none;
  height: 4px;
  border-radius: 2px;
  background: var(--bg-elevated);
  outline: none;
  cursor: pointer;
}

.settings__slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--accent);
  cursor: pointer;
  border: 2px solid var(--bg-primary);
}
```

**Account section:**
```css
.settings__account {
  display: flex;
  align-items: center;
  gap: 12px;
}

.settings__avatar {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-sm);
  background: var(--bg-elevated);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  flex-shrink: 0;
}

.settings__account-info {
  flex: 1;
  min-width: 0;
}

.settings__account-name {
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
}

.settings__auth-badge {
  display: inline-block;
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: 2px 6px;
  border-radius: 4px;
  margin-top: 2px;
}

.settings__auth-badge--microsoft {
  background: rgba(0, 120, 212, 0.2);
  color: #60a5fa;
}

.settings__auth-badge--offline {
  background: var(--bg-elevated);
  color: var(--text-muted);
}
```

**About / data path display:**
```css
.settings__data-path {
  font-family: var(--font-mono);
  font-size: 11px;
  color: var(--text-muted);
  background: var(--bg-elevated);
  padding: 6px 10px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border-subtle);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 100%;
  display: block;
  margin-top: 8px;
  margin-bottom: 12px;
}

.settings__about-actions {
  display: flex;
  gap: 8px;
}
```
  </action>
  <verify>File appended. No TypeScript compile errors (`npx tsc --noEmit`).</verify>
  <done>globals.css has a Settings page section with all required classes.</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsPage.tsx with five sections</name>
  <files>src/renderer/components/settings/SettingsPage.tsx</files>
  <action>
Create `src/renderer/components/settings/SettingsPage.tsx`.

**Structure and behaviour:**

```tsx
import React, { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuthStore } from '../../stores/auth-store';

export function SettingsPage() { ... }
```

**State:**
```tsx
const [settings, setSettings] = useState<LauncherSettings | null>(null);
const [appInfo, setAppInfo] = useState<{ version: string; dataPath: string } | null>(null);
const [cacheClearing, setCacheClearing] = useState(false);
const { profile, authMode, setProfile, setAuthMode } = useAuthStore();
const navigate = useNavigate();
```

**On mount:**
```tsx
useEffect(() => {
  window.launcher.settings.get().then(setSettings);
  window.launcher.settings.getAppInfo().then(setAppInfo);
}, []);
```

**Save helper:**
```tsx
function save<K extends keyof LauncherSettings>(key: K, value: LauncherSettings[K]) {
  window.launcher.settings.set(key, value);
  setSettings(prev => prev ? { ...prev, [key]: value } : prev);
}
```

**formatMemory helper:**
```tsx
function formatMemory(mb: number): string {
  return mb >= 1024 ? `${(mb / 1024).toFixed(1)} GB` : `${mb} MB`;
}
```

**Handle logout:**
```tsx
async function handleRelogin() {
  await window.launcher.auth.logout();
  setProfile(null);
  navigate('/login');
}
```

**Handle default auth mode toggle:**
```tsx
// Updates both settings persistence and auth store so UI reflects change
function handleDefaultAuthMode(mode: 'microsoft' | 'offline') {
  save('defaultAuthMode', mode);
  setAuthMode(mode);
}
```

**Handle clear cache:**
```tsx
async function handleClearCache() {
  if (!window.confirm('Clear cache (temp and natives directories)? The game will re-download these files on next launch.')) return;
  setCacheClearing(true);
  try {
    await window.launcher.settings.clearCache();
  } finally {
    setCacheClearing(false);
  }
}
```

**Render (return null while settings loading):**
If `!settings` return `null`.

**Five sections using CSS classes from Task 1:**

**1. Account section** (`.settings__section`):
- `.settings__account` div: avatar placeholder (`.settings__avatar` shows "👤"), profile name, auth badge (`.settings__auth-badge--microsoft` or `--offline`)
- Row: "Default auth mode" label + two radio-style buttons (or a toggle between "Microsoft" / "Offline") — use two `.btn.btn--sm` buttons where the active one has `.btn--primary` and inactive has `.btn--ghost`
- Row: "Switch Account" button (`.btn.btn--ghost.btn--sm`) calls `handleRelogin()`

**2. Java & Performance section:**
- `.settings__slider-row` for min memory: label "Minimum Memory", value badge shows `formatMemory(settings.defaultMinMemory)`, `<input type="range" className="settings__slider" min={512} max={8192} step={256} value={settings.defaultMinMemory} onChange={e => save('defaultMinMemory', +e.target.value)} />`
- `.settings__slider-row` for max memory: same pattern, min={512} max={16384} step={256}, key `defaultMaxMemory`
- `.settings__row` for Java path: label "Java Path Override", right side is `<input type="text" value={settings.javaPath} onChange={e => save('javaPath', e.target.value)} placeholder="Leave empty for auto-detect" style={{ background: 'var(--bg-elevated)', border: '1px solid var(--border)', borderRadius: 'var(--radius-sm)', color: 'var(--text-primary)', padding: '6px 10px', fontSize: 13, flex: 1 }} />`

**3. Game section:**
- `.settings__row` for close-on-launch: label "Close launcher when game launches" + toggle (`.settings__toggle` with `input[type=checkbox]` and `.settings__toggle-track`)
- `.settings__row` for resolution: label "Default Resolution" + two number inputs (width × height) side by side: `<input type="number" value={settings.defaultResolutionWidth} onChange={e => save('defaultResolutionWidth', +e.target.value)} style={{ width: 70, ...inputStyle }} />  ×  <input type="number" value={settings.defaultResolutionHeight} onChange={e => save('defaultResolutionHeight', +e.target.value)} style={{ width: 70, ...inputStyle }} />`

**4. BlockHaven section:**
- `.settings__row` host: label + text input for `blockhavenDefaultHost`
- `.settings__row` port: label + number input (width: 80px) for `blockhavenDefaultPort`
- Hint below section: "These values pre-fill the server field when creating new instances."

**5. About section:**
- Version row: "Launcher Version" label + `<span style={{ color: 'var(--text-secondary)', fontFamily: 'var(--font-mono)', fontSize: 12 }}>{appInfo?.version ?? '…'}</span>`
- Data directory: "Data Directory" label, then `.settings__data-path` showing `appInfo?.dataPath ?? '…'`
- `.settings__about-actions`: `<button className="btn btn--ghost btn--sm" onClick={() => window.launcher.settings.openDataFolder()}>Open Folder</button>` + `<button className="btn btn--danger btn--sm" onClick={handleClearCache} disabled={cacheClearing}>{cacheClearing ? 'Clearing…' : 'Clear Cache'}</button>`

**Inline input style constant** (reuse for small inputs):
```tsx
const inputStyle: React.CSSProperties = {
  background: 'var(--bg-elevated)',
  border: '1px solid var(--border)',
  borderRadius: 'var(--radius-sm)',
  color: 'var(--text-primary)',
  padding: '4px 8px',
  fontSize: 13,
};
```

Export as named export `SettingsPage`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>SettingsPage.tsx created. All 5 sections present. Compiles with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SettingsPage into App.tsx route</name>
  <files>src/renderer/App.tsx</files>
  <action>
In `src/renderer/App.tsx`:

1. Add import: `import { SettingsPage } from './components/settings/SettingsPage';`

2. Replace the placeholder route:
   ```tsx
   // Before:
   <Route path="/settings" element={<div>Settings (coming soon)</div>} />
   // After:
   <Route path="/settings" element={<SettingsPage />} />
   ```
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>App.tsx imports SettingsPage. /settings route renders the real component. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] globals.css has all settings CSS classes (settings, settings__section, settings__toggle, settings__slider, settings__account, etc.)
- [ ] SettingsPage.tsx exists at src/renderer/components/settings/SettingsPage.tsx
- [ ] All 5 sections render: Account, Java & Performance, Game, BlockHaven, About
- [ ] /settings route in App.tsx uses SettingsPage (not placeholder div)
- [ ] checkpoint:human-verify — open the Settings tab in the running app and visually confirm all sections render correctly
</verification>

<success_criteria>
- Settings page renders with all five sections
- Toggling default auth mode, sliders, and text fields call settings.set and persist on restart
- "Open Folder" opens the data directory
- "Clear Cache" shows confirmation dialog, then calls clearCache
- "Switch Account" logs out and navigates to /login
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/pcw-105-build-out-settings-tab/phases/02-settings-ui/02-01-SUMMARY.md`:

# Plan 02-01 Summary: Settings UI

**[one-line summary]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Plan Readiness
</output>
