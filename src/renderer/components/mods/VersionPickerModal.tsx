import React from 'react';

interface VersionPickerModalProps {
  mod: ModSearchHit;
  instanceId: string;
  instances: InstanceInfo[];
  onClose: () => void;
  onInstalled: () => void;
}

export function VersionPickerModal(_props: VersionPickerModalProps): null {
  return null;
}
